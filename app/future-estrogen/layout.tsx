import { SiteChrome } from '@/components/layout/SiteChrome'

export default async function FutureEstrogenLayout({ children }: { children: React.ReactNode }) {
  return <SiteChrome>{children}</SiteChrome>
}
